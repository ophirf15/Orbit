<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Orbit_App.Controls.CommandPalette"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Border Background="#99000000" PointerPressed="Backdrop_PointerPressed" />

        <Border
            MaxWidth="560"
            Margin="24,48,24,24"
            Padding="12"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBox
                    x:Name="QueryBox"
                    AutomationProperties.Name="Command palette search"
                    KeyDown="QueryBox_KeyDown"
                    PlaceholderText="Type a command…"
                    TextChanged="QueryBox_TextChanged" />

                <ListView
                    x:Name="ResultsList"
                    Grid.Row="1"
                    MaxHeight="320"
                    Margin="0,8,0,0"
                    AutomationProperties.Name="Command results"
                    DoubleTapped="ResultsList_DoubleTapped"
                    IsItemClickEnabled="True"
                    ItemClick="ResultsList_ItemClick"
                    KeyDown="ResultsList_KeyDown"
                    SelectionMode="Single">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Padding="4,8" Text="{Binding Title}" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <TextBlock
                    x:Name="EmptyState"
                    Grid.Row="2"
                    Margin="4,8,4,0"
                    Opacity="0.7"
                    Text="No matching commands"
                    Visibility="Collapsed" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
