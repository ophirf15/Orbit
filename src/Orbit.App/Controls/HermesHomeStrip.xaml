<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Orbit_App.Controls.HermesHomeStrip"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border
        Padding="14,12"
        Background="{ThemeResource OrbitDetailSurfaceBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="{StaticResource ControlCornerRadius}">
        <Grid RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="4">
                    <TextBlock
                        FontWeight="SemiBold"
                        Opacity="0.85"
                        Text="From Hermes" />
                    <TextBlock
                        x:Name="DayBriefText"
                        LineHeight="20"
                        MaxLines="4"
                        Text="Loading…"
                        TextTrimming="CharacterEllipsis"
                        TextWrapping="Wrap" />
                    <TextBlock
                        x:Name="HermesHintText"
                        Opacity="0.55"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        TextWrapping="Wrap"
                        Visibility="Collapsed" />
                    <TextBlock
                        x:Name="ActivityText"
                        Opacity="0.7"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        Text=""
                        TextWrapping="Wrap" />
                </StackPanel>
                <Button
                    x:Name="RefreshButton"
                    Grid.Column="1"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Refresh Hermes pulse"
                    Click="RefreshButton_Click"
                    Content="Refresh"
                    ToolTipService.ToolTip="Ask Hermes for a duty scan and reload">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="F5" />
                    </Button.KeyboardAccelerators>
                </Button>
            </Grid>

            <TextBlock
                x:Name="ConcernsHeader"
                Grid.Row="1"
                FontWeight="SemiBold"
                Opacity="0.85"
                Text="Needs you now" />

            <ScrollViewer
                Grid.Row="2"
                MaxHeight="140"
                HorizontalScrollBarVisibility="Auto"
                HorizontalScrollMode="Enabled"
                VerticalScrollBarVisibility="Disabled"
                VerticalScrollMode="Disabled">
                <ItemsRepeater x:Name="ConcernsRepeater">
                    <ItemsRepeater.Layout>
                        <StackLayout Orientation="Horizontal" Spacing="8" />
                    </ItemsRepeater.Layout>
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Button
                                MinWidth="180"
                                MaxWidth="260"
                                Padding="12,10"
                                Click="ConcernChip_Click"
                                HorizontalContentAlignment="Left"
                                Tag="{Binding TaskId}">
                                <StackPanel Spacing="2">
                                    <TextBlock
                                        FontWeight="SemiBold"
                                        Text="{Binding Title}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="NoWrap" />
                                    <TextBlock
                                        Opacity="0.7"
                                        Style="{StaticResource CaptionTextBlockStyle}"
                                        Text="{Binding SubtitleLine}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="NoWrap" />
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
