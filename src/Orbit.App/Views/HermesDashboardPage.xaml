<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Orbit_App.Views.HermesDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock
                Style="{StaticResource TitleTextBlockStyle}"
                Text="Hermes dashboard"
                VerticalAlignment="Center" />
            <Button
                VerticalAlignment="Center"
                Click="OpenExternal_Click"
                Content="Open in browser" />
            <Button
                VerticalAlignment="Center"
                Click="Reload_Click"
                Content="Reload" />
        </StackPanel>

        <TextBlock
            x:Name="HintText"
            Grid.Row="1"
            Opacity="0.85"
            TextWrapping="Wrap"
            Text="Provider login, Telegram, and mail/calendar skills live here (:9119). If OAuth fails inside the pane, use Open in browser." />

        <WebView2
            x:Name="DashboardView"
            Grid.Row="2"
            DefaultBackgroundColor="Transparent" />
    </Grid>
</Page>
