<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="Orbit_App.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Orbit_App.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16,8,16,12" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8">
            <FontIcon Glyph="&#xE721;" FontSize="18" VerticalAlignment="Center" />
            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Search" VerticalAlignment="Center" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                x:Name="QueryBox"
                AutomationProperties.Name="Search query"
                KeyDown="QueryBox_KeyDown"
                PlaceholderText="Search projects, people, files, emails…" />
            <Button
                Grid.Column="1"
                AutomationProperties.Name="Run search"
                Click="Search_Click"
                Content="Search" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="1.1*" />
            </Grid.ColumnDefinitions>

            <ListView
                x:Name="ResultsList"
                AutomationProperties.Name="Search results"
                IsItemClickEnabled="True"
                ItemClick="ResultsList_ItemClick"
                SelectionMode="Single">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Padding="4,6" Spacing="2">
                            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{Binding Title}" />
                            <TextBlock
                                Opacity="0.75"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{Binding MetaLine}" />
                            <TextBlock
                                Opacity="0.65"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Text="{Binding Snippet}"
                                TextWrapping="WrapWholeWords" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border
                Grid.Column="1"
                Padding="12"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource ControlCornerRadius}">
                <Grid RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock
                        x:Name="PreviewTitle"
                        Style="{StaticResource BodyStrongTextBlockStyle}"
                        Text="Preview" />
                    <controls:FilePreviewControl x:Name="PreviewHost" Grid.Row="1" />
                </Grid>
            </Border>
        </Grid>

        <TextBlock
            x:Name="StatusText"
            Grid.Row="3"
            Opacity="0.75"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="Type a fragment and press Search." />
    </Grid>
</Page>
